/**
 * @fileoverview Vite client type declarations.
 *
 * Provides TypeScript with type stubs for Vite-handled asset imports
 * (CSS files, images, etc.) so the compiler accepts them without error.
 */

/// <reference types="vite/client" />
