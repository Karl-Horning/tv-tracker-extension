{
  "name": "tv-tracker-extension",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.app.json && tsc -p tsconfig.node.json && vite build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.0",
    "@types/node": "^26.0.1",
    "typescript": "^6.0.3",
    "vite": "^8.1.2",
    "vitest": "^3.2.6"
  }
}
